# README

Use recursive algorithm to solve a sudoku puzzle.

The advantages of recursive algorithm:

1. concise

2. simple state management - the states are kept in the recursive call structure implicitly.
One of the advantages in using a recursive algorithm is the states are captured in the recursive structure
The logic to iterate through the 9-cell block is coded by the VS Code AI.

